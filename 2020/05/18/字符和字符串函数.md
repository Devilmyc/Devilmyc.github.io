## 字符串
```
int main(){
    //字符串的声明方式
    //字符串是使用 00 \0作为结尾的
    //gets puts 输入输出 fgets fputs 同样
    char szStr0[50] ="my name is zs";
    puts(szStr0);
    char szStr1[50] = {'m','y',' ','n','a','m','e',' ','i','s',' ','z','s','\0'};
    puts(szStr1);
    char szStr2[] = "my name is zs";
    puts(szStr2);
    char * szStr3 ="my name is zs";
    puts(szStr3);

    char szStr4[50];
    gets(szStr4);
    return 0;
}

///
char szArrStr[50];
fgets(szStr,50,stdin);//stdin 标准输入
fputs(szStr,stdout);//stdout 标准输出
///
char szStr5[50]="my name is zs";
char szStr6[50] = " Hello";
int len = strlen(szStr5);
strcat(szStr5,szStr6);
strncat(szStr5,szStr6,参数)；//参数的意思在于限制拼接的第二个字符串取得位数
//测量字符串的长度
//strcat 字符串的拼接函数
//strncat 字符串的拼接函数
//strcmp 对比两个字符串
//strncmp
//strcpy 赋值字符串
//strncpy
//sprintf 将一个字符串打印到另一个字符串中
```